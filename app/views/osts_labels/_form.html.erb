<%= form_with(model: osts_label) do |form| %>
  <% if osts_label.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(osts_label.errors.count, "error") %> prohibited this osts_label from being saved:</h2>

      <ul>
        <% osts_label.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
