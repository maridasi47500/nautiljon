<%= form_with(model: artbooks_jv) do |form| %>
  <% if artbooks_jv.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_jv.errors.count, "error") %> prohibited this artbooks_jv from being saved:</h2>

      <ul>
        <% artbooks_jv.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
