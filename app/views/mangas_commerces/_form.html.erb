<%= form_with(model: _mangas_commerce) do |form| %>
  <% if _mangas_commerce.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_mangas_commerce.errors.count, "error") %> prohibited this _mangas_commerce from being saved:</h2>

      <ul>
        <% _mangas_commerce.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
