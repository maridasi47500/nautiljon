<%= form_with(model: osts_am) do |form| %>
  <% if osts_am.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(osts_am.errors.count, "error") %> prohibited this osts_am from being saved:</h2>

      <ul>
        <% osts_am.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
