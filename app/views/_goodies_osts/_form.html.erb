<%= form_with(model: _goodies_ost) do |form| %>
  <% if _goodies_ost.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_goodies_ost.errors.count, "error") %> prohibited this _goodies_ost from being saved:</h2>

      <ul>
        <% _goodies_ost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
