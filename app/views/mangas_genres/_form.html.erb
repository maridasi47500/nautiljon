<%= form_with(model: _mangas_genre) do |form| %>
  <% if _mangas_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_mangas_genre.errors.count, "error") %> prohibited this _mangas_genre from being saved:</h2>

      <ul>
        <% _mangas_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
