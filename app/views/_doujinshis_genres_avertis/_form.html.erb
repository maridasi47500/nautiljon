<%= form_with(model: _doujinshis_genres_averti) do |form| %>
  <% if _doujinshis_genres_averti.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_doujinshis_genres_averti.errors.count, "error") %> prohibited this _doujinshis_genres_averti from being saved:</h2>

      <ul>
        <% _doujinshis_genres_averti.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
