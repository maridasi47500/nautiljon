<%= form_with(model: amvs_convention) do |form| %>
  <% if amvs_convention.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(amvs_convention.errors.count, "error") %> prohibited this amvs_convention from being saved:</h2>

      <ul>
        <% amvs_convention.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
