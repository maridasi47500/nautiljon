<%= form_with(model: _recettes_difficulte) do |form| %>
  <% if _recettes_difficulte.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_recettes_difficulte.errors.count, "error") %> prohibited this _recettes_difficulte from being saved:</h2>

      <ul>
        <% _recettes_difficulte.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
