<%= form_with(model: animes_culture) do |form| %>
  <% if animes_culture.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animes_culture.errors.count, "error") %> prohibited this animes_culture from being saved:</h2>

      <ul>
        <% animes_culture.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
