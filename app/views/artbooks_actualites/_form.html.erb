<%= form_with(model: artbooks_actualite) do |form| %>
  <% if artbooks_actualite.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_actualite.errors.count, "error") %> prohibited this artbooks_actualite from being saved:</h2>

      <ul>
        <% artbooks_actualite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
