<%= form_with(model: evenements_pay) do |form| %>
  <% if evenements_pay.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(evenements_pay.errors.count, "error") %> prohibited this evenements_pay from being saved:</h2>

      <ul>
        <% evenements_pay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
