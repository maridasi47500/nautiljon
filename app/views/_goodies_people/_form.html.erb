<%= form_with(model: _goodies_person) do |form| %>
  <% if _goodies_person.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_goodies_person.errors.count, "error") %> prohibited this _goodies_person from being saved:</h2>

      <ul>
        <% _goodies_person.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
