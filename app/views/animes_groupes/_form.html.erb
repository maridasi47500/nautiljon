<%= form_with(model: animes_groupe) do |form| %>
  <% if animes_groupe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animes_groupe.errors.count, "error") %> prohibited this animes_groupe from being saved:</h2>

      <ul>
        <% animes_groupe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
