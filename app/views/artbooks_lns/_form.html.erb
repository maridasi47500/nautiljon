<%= form_with(model: artbooks_ln) do |form| %>
  <% if artbooks_ln.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_ln.errors.count, "error") %> prohibited this artbooks_ln from being saved:</h2>

      <ul>
        <% artbooks_ln.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
