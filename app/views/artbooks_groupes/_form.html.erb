<%= form_with(model: artbooks_groupe) do |form| %>
  <% if artbooks_groupe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_groupe.errors.count, "error") %> prohibited this artbooks_groupe from being saved:</h2>

      <ul>
        <% artbooks_groupe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
