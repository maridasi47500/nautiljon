<%= form_with(model: _boutiques_pay) do |form| %>
  <% if _boutiques_pay.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_boutiques_pay.errors.count, "error") %> prohibited this _boutiques_pay from being saved:</h2>

      <ul>
        <% _boutiques_pay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
