<%= form_with(model: artbooks_anime) do |form| %>
  <% if artbooks_anime.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_anime.errors.count, "error") %> prohibited this artbooks_anime from being saved:</h2>

      <ul>
        <% artbooks_anime.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
