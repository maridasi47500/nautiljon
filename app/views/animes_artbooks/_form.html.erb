<%= form_with(model: animes_artbook) do |form| %>
  <% if animes_artbook.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animes_artbook.errors.count, "error") %> prohibited this animes_artbook from being saved:</h2>

      <ul>
        <% animes_artbook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
