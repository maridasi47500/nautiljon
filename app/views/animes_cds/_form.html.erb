<%= form_with(model: animes_cd) do |form| %>
  <% if animes_cd.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animes_cd.errors.count, "error") %> prohibited this animes_cd from being saved:</h2>

      <ul>
        <% animes_cd.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
