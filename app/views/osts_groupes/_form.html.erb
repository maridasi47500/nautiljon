<%= form_with(model: osts_groupe) do |form| %>
  <% if osts_groupe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(osts_groupe.errors.count, "error") %> prohibited this osts_groupe from being saved:</h2>

      <ul>
        <% osts_groupe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
