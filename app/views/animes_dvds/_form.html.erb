<%= form_with(model: animes_dvd) do |form| %>
  <% if animes_dvd.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animes_dvd.errors.count, "error") %> prohibited this animes_dvd from being saved:</h2>

      <ul>
        <% animes_dvd.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
