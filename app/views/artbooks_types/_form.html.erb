<%= form_with(model: artbooks_type) do |form| %>
  <% if artbooks_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_type.errors.count, "error") %> prohibited this artbooks_type from being saved:</h2>

      <ul>
        <% artbooks_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
