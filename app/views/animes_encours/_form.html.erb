<%= form_with(model: animes_encour) do |form| %>
  <% if animes_encour.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animes_encour.errors.count, "error") %> prohibited this animes_encour from being saved:</h2>

      <ul>
        <% animes_encour.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
