<%= form_with(model: artbooks_drama) do |form| %>
  <% if artbooks_drama.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_drama.errors.count, "error") %> prohibited this artbooks_drama from being saved:</h2>

      <ul>
        <% artbooks_drama.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
