<%= form_with(model: osts_drama) do |form| %>
  <% if osts_drama.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(osts_drama.errors.count, "error") %> prohibited this osts_drama from being saved:</h2>

      <ul>
        <% osts_drama.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
