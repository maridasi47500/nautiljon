<%= form_with(model: amvs_amv) do |form| %>
  <% if amvs_amv.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(amvs_amv.errors.count, "error") %> prohibited this amvs_amv from being saved:</h2>

      <ul>
        <% amvs_amv.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
