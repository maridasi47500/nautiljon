<%= form_with(model: _doujins_convention) do |form| %>
  <% if _doujins_convention.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_doujins_convention.errors.count, "error") %> prohibited this _doujins_convention from being saved:</h2>

      <ul>
        <% _doujins_convention.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
