<%= form_with(model: amvs_anime) do |form| %>
  <% if amvs_anime.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(amvs_anime.errors.count, "error") %> prohibited this amvs_anime from being saved:</h2>

      <ul>
        <% amvs_anime.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
