<%= form_with(model: _personnalites_role) do |form| %>
  <% if _personnalites_role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_personnalites_role.errors.count, "error") %> prohibited this _personnalites_role from being saved:</h2>

      <ul>
        <% _personnalites_role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
