<%= form_with(model: _volumes_manga) do |form| %>
  <% if _volumes_manga.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_volumes_manga.errors.count, "error") %> prohibited this _volumes_manga from being saved:</h2>

      <ul>
        <% _volumes_manga.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
