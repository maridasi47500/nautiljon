<%= form_with(model: artbooks_goody) do |form| %>
  <% if artbooks_goody.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_goody.errors.count, "error") %> prohibited this artbooks_goody from being saved:</h2>

      <ul>
        <% artbooks_goody.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
