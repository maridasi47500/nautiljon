<%= form_with(model: artbooks_artbook) do |form| %>
  <% if artbooks_artbook.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artbooks_artbook.errors.count, "error") %> prohibited this artbooks_artbook from being saved:</h2>

      <ul>
        <% artbooks_artbook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
