<%= form_with(model: _videos_id_cat) do |form| %>
  <% if _videos_id_cat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_videos_id_cat.errors.count, "error") %> prohibited this _videos_id_cat from being saved:</h2>

      <ul>
        <% _videos_id_cat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
